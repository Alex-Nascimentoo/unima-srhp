[tool.poetry]
name = "unima-srhp"
version = "0.1.0"
description = ""
authors = ["Seu Nome <seu@email.com>"]

packages = [
    { include = "core", from = "src" },
    { include = "model", from = "src" },
    { include = "modules", from = "src" }
]